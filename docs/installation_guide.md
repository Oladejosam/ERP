# Installation Guide

1. Start Apache and MySQL in XAMPP.
2. Open http://localhost/ERP/install.php to import the SQL scripts.
3. Visit http://localhost/ERP/public/ to open the dashboard.
4. Use the default admin login after creating a user in the database.
